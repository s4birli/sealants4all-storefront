import { StubPage } from "@/components/layout/StubPage";

export default function CheckoutPage() {
  return (
    <StubPage
      title="Checkout"
      description="4-step accordion checkout (Contact → Delivery → Payment → Review) wired to Medusa.js and Stripe arrives in Phase 1.5."
    />
  );
}
