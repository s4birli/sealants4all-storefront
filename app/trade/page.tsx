import { StubPage } from "@/components/layout/StubPage";

export default function TradePage() {
  return (
    <StubPage
      title="Trade Hub"
      description="Apply for a trade account, get net-30 terms, and unlock bulk tier pricing. The full trade portal — saved order lists, dashboards, named account rep — lands in Phase 2."
    />
  );
}
