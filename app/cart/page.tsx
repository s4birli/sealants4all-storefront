import { StubPage } from "@/components/layout/StubPage";

export default function CartPage() {
  return (
    <StubPage
      title="Your basket"
      description="The full cart page is the drawer entry. A dedicated /cart route with multi-warehouse split shipping arrives in Phase 1.5."
    />
  );
}
